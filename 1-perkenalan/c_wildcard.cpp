#include <iostream>
#include <string>

using namespace std;

int main(){
  string pattern; cin >> pattern;
  int n; cin >> n;

  while (n--) {
    string curr; cin >> curr;
    int flag = 0;



    if (flag == 1) {
      cout << curr << "\n";
    }
  }

  return 0;
}
